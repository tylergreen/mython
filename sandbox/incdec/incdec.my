quote [myfront]:
    def incdec(x,y):
        if y == '+':
            return x + 1 
        elif y == '-':
            return x - 1

    def myinterp(string):
        return reduce(incdec, list(string.strip()), 0)
    
    i = makequote(myinterp)

quote [i] result:
    +++++----++

if __name__ == "__main__":
    print result + 100
    

    
